# Singer Pipedrive tap and PostgreSQL target Docker Image

## How to build Docker Image

```
$ ./scripts/build-docker-image.sh
```

## How to test stephaneklein/singer-pipedrive-postgresql Docker Image

```
$ docker-compose up -d
```